// Entry das páginas públicas (layout "landing"): LP, Thank You, recuperação de acesso.
// Mantido mínimo: apenas o loader; checkout, tracking, attribution e sticky_cta são carregados
// sob demanda quando o HTML declara data-module.
import { activate } from "lib/modules";

activate();
